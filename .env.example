# Optional: used for canonical, robots, and sitemap absolute URLs.
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional: server-side webhook endpoint for quote submissions (Make/n8n/etc).
QUOTE_WEBHOOK_URL=
